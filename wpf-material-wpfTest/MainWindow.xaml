<Window x:Class="wpf_material_wpfTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
        <materialDesign:DialogHost Identifier="AlertDialog" CloseOnClickAway="False"  DialogTheme="Inherit" Style="{StaticResource MaterialDesignEmbeddedDialogHost}"
                                   xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                                   Foreground="{DynamicResource AttentionToActionBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Command="{Binding OpenDialog, RelativeSource={RelativeSource AncestorType={x:Type Window}}}">Open Test dialog</Button>
                <Button Grid.Column="1" Command="{Binding OpenDialog2, RelativeSource={RelativeSource AncestorType={x:Type Window}}}">Open Test dialog XAML</Button>
            </Grid>
        </materialDesign:DialogHost>
</Window>
